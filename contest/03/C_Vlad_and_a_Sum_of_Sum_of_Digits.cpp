#include <bits/stdc++.h>

using namespace std;

int main()
{

    long long prefix[200005];
    // long long par[200005];

    // memset(prefix, -1, sizeof(prefix));
    // int s = 1;
    // for (int i = 1; i < 200005; i++)
    // {
    //     if (s == 10)
    //     {
    //         s = 1;
    //     }
    //     par[i] = s;
    //     s++;
    // }

    // long long sum = 0;

    // for (int i = 1; i < 200005; i++)
    // {
    //     prefix[i] = par[i] + sum;
    //     sum = prefix[i];
    // }

    int t;
    cin >> t;

    while (t--)
    {
        int inp;
        cin >> inp;

        cout << "hello => " << inp << endl;
    }

    return 0;
}