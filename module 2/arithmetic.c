#include <stdio.h>

int main()
{

    int a, b;
    a = 10;
    b = 20;

    int sum = a + b;
    printf("%d \n", sum);

    int sub = a - b;
    printf("%d \n", sub);

    int mul = a * b;
    printf("%d \n", mul);

    int div = a / b;
    printf("%d \n", div);

    int mod = a * 2 / b;
    printf("%d \n", mod);

    int x = 1;

    if (x = 2)
    {
        printf("hello inside");
    }
    else
    {
        printf("hello outside");
    }

    return 0;
}