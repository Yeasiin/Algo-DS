#include <stdio.h>

int main()
{

    int n;

    scanf("%d", &n);

    int offFac[n];

    for (int i = 0; i < n; i++)
    {
        scanf("%d", &offFac[i]);
    }
    int result[n];
    for (int i = 0; i < n; i++)
    {
    }

    for (int i = 0; i < n; i++)
    {
        printf("%d --\n", result[i]);
    }

    return 0;
}